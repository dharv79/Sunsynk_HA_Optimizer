# Raw data exports - can get large, keep out of git
data/forecasts/*.csv
data/forecasts/*.json
data/actuals/*.csv
data/actuals/*.json

# Python cache — generated by py_compile / interpreter, never committed
__pycache__/
*.pyc

# Windows filesystem metadata — match everywhere in the tree
[Dd]esktop.ini
Thumbs.db
